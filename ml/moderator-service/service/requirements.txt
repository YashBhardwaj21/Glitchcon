# ── Web framework ─────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart          # form data / file uploads in FastAPI

# ── Config & validation ───────────────────────────────────
pydantic
pydantic-settings
python-dotenv             # load .env in development

# ── Database ──────────────────────────────────────────────
sqlalchemy[asyncio]       # asyncio extra enables async engine
asyncpg                   # async PostgreSQL driver
alembic

# ── Cache & task queue ────────────────────────────────────
redis                     # async Redis client (aioredis merged into redis>=4)
celery[redis]             # [redis] adds broker + backend support
flower                    # Celery monitoring dashboard

# ── Auth ──────────────────────────────────────────────────
python-jose[cryptography] # JWT tokens — [cryptography] backend required
passlib[bcrypt]           # password / API key hashing — [bcrypt] extra required
bcrypt                    # passlib[bcrypt] pulls this, but pin it explicitly

# ── LLM providers ─────────────────────────────────────────
groq                      # Groq async client (Llama-3)
google-generativeai       # Gemini provider
openai                    # OpenRouter uses openai-compatible API
aiohttp                   # required internally by google-generativeai

# ── AI / ML pipeline ──────────────────────────────────────
sentence-transformers     # paraphrase-multilingual-MiniLM-L12-v2
faiss-cpu                 # FAISS vector index (CPU); swap to faiss-gpu if CUDA available
numpy                     # required by faiss-cpu and sentence-transformers

# ── Multilingual / NLP ────────────────────────────────────
langdetect                # language detection (Stage 0)
indic-transliteration     # Roman-script Indic → Unicode normalisation (Stage 0)
better-profanity          # English profanity filter (Stage 1)

# ── Templating ────────────────────────────────────────────
jinja2                    # feedback message templates

# ── HTTP client (SDK) ─────────────────────────────────────
httpx                     # async HTTP client for the Python SDK

# ── Testing ───────────────────────────────────────────────
pytest
pytest-asyncio
pytest-cov
